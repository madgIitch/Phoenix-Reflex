# Demo Assets

Captured trace sessions from live runs of the correction loop and MCP introspection beats. Each file is a raw session export showing `phantom_citations_detected_count`, `failure_mode`, `improvement_case`, and MCP tool calls — use as backup evidence if a live demo run does not produce the ideal case.
